<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="orange.service.impl.InquiryMapper">

	<select id="selectInquiryList" parameterType="inquiryVO" resultType="egovMap">
		SELECT 
				unq,
				title,
				category,
				content,
				to_char(rdate,'yyyy-mm-dd') rdate,
				decode(status,'N','답변대기','답변완료') status,
				nik_name
		FROM INQUIRY_BOARD
	</select>
	
	<insert id="insertInquiry" parameterType="inquiryVO">
		INSERT INTO INQUIRY_BOARD 
					(unq,
					title,
					category,
					content,
					rdate,
					udate,
					writer,
					nik_name) 
			VALUES (INQUIRY_BOARD_SEQ.NEXTVAL,
					#{title},
					#{category},
					#{content},
					sysdate,
					sysdate,
					#{writer},
					#{nikName})
	</insert>
</mapper>